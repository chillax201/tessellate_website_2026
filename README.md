this is the git repository for the website of CMI's tessellate 2026


for those contributing to the pictures and blurbs of events:
 1.duplicate [about.html](./about.html)
 2.rename to `event_blurb.html` and edit text in `<p>`.
 3.add an `<a href="./event_blurb.html"  class="normal-link">` tag in the corresponding `day_i.html` file's event.
 4.to add events in `day_i.html` kindly follow convention used for other event listings.
